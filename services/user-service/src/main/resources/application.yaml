server:
  port: 5001
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/airline_location_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update